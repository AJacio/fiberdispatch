# FiberDispatch

A scheduling dashboard prototype for a fiber internet installation company —
technician dispatch, condo building/unit tracking, and job status management.

Built with React, Vite, and Tailwind CSS. Currently runs on in-memory mock
data (no backend yet).

## Run locally

npm install
npm run dev

## Build

npm run build
